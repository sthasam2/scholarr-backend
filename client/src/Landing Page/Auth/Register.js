import React from "react";
import { motion } from "framer-motion";

const Register = () => {
  return (
    <motion.h1 exit={{ opacity: 0 }} className="register">
      Register
    </motion.h1>
  );
};

export default Register;
